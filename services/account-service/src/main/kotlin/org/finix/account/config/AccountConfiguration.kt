package org.finix.account.config

import org.springframework.context.annotation.Configuration

/** Spring wiring for account-service — keeps the Configuration ArchUnit layer non-empty. */
@Configuration
class AccountConfiguration
